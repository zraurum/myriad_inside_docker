# Детекция объектов с веб-камеры через OpenVINO и YOLO

Проект для детекции объектов в реальном времени с использованием веб-камеры, YOLO модели и OpenVINO для ускорения inference на различных устройствах (CPU, MYRIAD, GPU).

## 📋 Содержание

- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Использование](#использование)
- [Переменные окружения](#переменные-окружения)
- [Примеры](#примеры)
- [Решение проблем](#решение-проблем)

## ✨ Возможности

- 🎥 Детекция объектов в реальном времени с веб-камеры
- 🚀 Поддержка OpenVINO для ускорения inference на CPU, MYRIAD, GPU
- 💾 Автоматическое сохранение видео с детекциями
- 📊 Использование YOLOv8 для детекции объектов
- 🐳 Готовый Docker образ с предустановленными зависимостями
- 🔄 Автоматическая конвертация PyTorch моделей в OpenVINO формат

## 📦 Требования

### Для хоста (система, где запускается Docker):

- **Docker** версии 20.10 или выше
- **Docker Compose** (опционально, для удобства)
- **Веб-камера**, подключенная к системе
- **Linux** (рекомендуется Ubuntu 20.04+)

### Для работы с MYRIAD (Intel Neural Compute Stick):

- Intel Neural Compute Stick 2 (NCS2) или совместимое устройство
- Правильно настроенные USB права доступа

## 🛠 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd myriad_test
```

### 2. Сборка Docker образа

Соберите Docker образ из Dockerfile:

```bash
docker build -t webcam-detection:latest .
```

Этот процесс может занять несколько минут, так как:
- Загружается базовый образ OpenVINO
- Устанавливаются системные пакеты
- Устанавливаются Python зависимости (ultralytics, numpy и др.)

### 3. Проверка веб-камеры

Убедитесь, что веб-камера доступна в системе:

```bash
# Проверка доступных видеоустройств
ls -l /dev/video*

# Или используйте v4l2 для получения информации
v4l2-ctl --list-devices
```

## 🚀 Использование

### Базовый запуск

Самый простой способ запустить детекцию:

```bash
docker run --rm \
  --device=/dev/video0 \
  -v /tmp/output:/workspace/output \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

### Запуск с MYRIAD устройством

Если у вас есть Intel Neural Compute Stick:

```bash
docker run --rm \
  --device=/dev/video0 \
  --device=/dev/dri \
  -v /tmp/output:/workspace/output \
  -e DEVICE=MYRIAD \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

### Запуск с сохранением видео

Для сохранения видео с детекциями на хост:

```bash
docker run --rm \
  --device=/dev/video0 \
  -v $(pwd)/output:/workspace/output \
  -v $(pwd)/host_output:/host_output \
  -e SAVE_OUTPUT=true \
  -e HOST_OUTPUT_DIR=/host_output \
  -e SAVE_AFTER_FRAMES=100 \
  -e STOP_AFTER_SAVE=true \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

## ⚙️ Переменные окружения

Скрипт поддерживает следующие переменные окружения для настройки поведения:

| Переменная | Описание | Значение по умолчанию | Пример |
|------------|----------|----------------------|--------|
| `DEVICE` | Устройство для inference | `MYRIAD` | `CPU`, `MYRIAD`, `GPU` |
| `SAVE_OUTPUT` | Сохранять ли видео | `true` (если не задано) | `true`, `false` |
| `OUTPUT_DIR` | Директория внутри контейнера для сохранения | Текущая директория | `/workspace/output` |
| `HOST_OUTPUT_DIR` | Директория хоста для копирования результата | Не задано | `/host_output` |
| `SAVE_AFTER_FRAMES` | Количество кадров до начала сохранения | `100` | `50`, `200`, `0` (сразу) |
| `STOP_AFTER_SAVE` | Останавливать ли запись после достижения лимита | `true` | `true`, `false` |

### Подробное описание переменных

#### `DEVICE`
Определяет устройство для выполнения inference:
- **CPU**: Использует процессор (самый медленный, но всегда доступен)
- **MYRIAD**: Использует Intel Neural Compute Stick (требует подключенное устройство)
- **GPU**: Использует графический процессор (требует поддержку OpenVINO GPU)

#### `SAVE_OUTPUT`
- `true`: Видео будет сохраняться
- `false`: Видео не сохраняется (только вывод в консоль)

#### `OUTPUT_DIR`
Путь внутри контейнера, где будет создан видеофайл. Рекомендуется использовать примонтированный том для доступа с хоста.

#### `HOST_OUTPUT_DIR`
Если задано, после завершения записи видео будет скопировано в эту директорию на хосте. Должна быть примонтирована как volume.

#### `SAVE_AFTER_FRAMES`
- `0`: Начинать сохранение сразу с первого кадра
- `> 0`: Начинать сохранение после обработки указанного количества кадров
- По умолчанию: `100` кадров

#### `STOP_AFTER_SAVE`
- `true`: Остановить выполнение сразу после достижения лимита кадров (`SAVE_AFTER_FRAMES`)
- `false`: Продолжать работу после достижения лимита

## 📝 Примеры

### Пример 1: Быстрый тест с CPU

```bash
docker run --rm \
  --device=/dev/video0 \
  -e DEVICE=CPU \
  -e SAVE_OUTPUT=false \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

### Пример 2: Запись 200 кадров с автоматической остановкой

```bash
docker run --rm \
  --device=/dev/video0 \
  -v $(pwd)/output:/workspace/output \
  -e SAVE_OUTPUT=true \
  -e SAVE_AFTER_FRAMES=200 \
  -e STOP_AFTER_SAVE=true \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

### Пример 3: Непрерывная запись с сохранением на хост

```bash
docker run --rm \
  --device=/dev/video0 \
  -v $(pwd)/output:/workspace/output \
  -v $(pwd)/host_output:/host_output \
  -e SAVE_OUTPUT=true \
  -e SAVE_AFTER_FRAMES=0 \
  -e STOP_AFTER_SAVE=false \
  -e HOST_OUTPUT_DIR=/host_output \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

Для остановки нажмите `Ctrl+C`.

### Пример 4: Использование другой веб-камеры

Если у вас несколько камер, укажите нужную через монтирование:

```bash
docker run --rm \
  --device=/dev/video1 \
  -v $(pwd)/output:/workspace/output \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

## 🔧 Решение проблем

### Проблема: "Не удалось открыть веб-камеру"

**Решение:**
1. Проверьте, что камера подключена: `ls -l /dev/video*`
2. Убедитесь, что передаете правильное устройство: `--device=/dev/video0`
3. Проверьте права доступа: `sudo chmod 666 /dev/video0`

### Проблема: "Не удалось инициализировать VideoWriter"

**Решение:**
1. Убедитесь, что директория для сохранения существует и доступна для записи
2. Проверьте, что том правильно примонтирован: `-v $(pwd)/output:/workspace/output`
3. Убедитесь, что у контейнера есть права на запись в директорию

### Проблема: MYRIAD устройство не определяется

**Решение:**
1. Проверьте подключение устройства: `lsusb | grep Intel`
2. Убедитесь, что передаете устройство: `--device=/dev/dri`
3. Проверьте права доступа к USB устройствам
4. Попробуйте запустить с `--privileged` (не рекомендуется для продакшена):

```bash
docker run --rm --privileged \
  --device=/dev/video0 \
  -e DEVICE=MYRIAD \
  webcam-detection:latest \
  python3 /workspace/webcam_detection.py
```

### Проблема: Видео сохраняется только с одним кадром

**Решение:**
1. Убедитесь, что `SAVE_AFTER_FRAMES` установлен правильно
2. Проверьте, что скрипт не останавливается слишком рано
3. Убедитесь, что `video_fps` установлен корректно (не 1 FPS)

### Проблема: Файл не появляется на хосте

**Решение:**
1. Убедитесь, что том примонтирован: `-v $(pwd)/output:/workspace/output`
2. Если используете `HOST_OUTPUT_DIR`, проверьте, что том примонтирован: `-v $(pwd)/host_output:/host_output`
3. Проверьте права доступа к директории на хосте: `chmod 777 output`

### Проблема: Медленная работа

**Решение:**
1. Используйте MYRIAD устройство для ускорения: `-e DEVICE=MYRIAD`
2. Уменьшите разрешение камеры (измените в коде `cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)`)
3. Используйте более легкую модель YOLO (например, `yolov8n` вместо `yolov8m`)

### Проблема: Ошибки при сборке Docker образа

**Решение:**
1. Убедитесь, что у вас достаточно места на диске
2. Проверьте подключение к интернету (нужно для загрузки базового образа)
3. Попробуйте очистить Docker кэш: `docker system prune -a`

## 📊 Структура проекта

```
myriad_test/
├── dockerfile              # Docker образ с OpenVINO и зависимостями
├── entrypoint.sh           # Скрипт для активации OpenVINO окружения
├── webcam_detection.py     # Основной скрипт детекции
├── README.md               # Этот файл
└── output/                 # Директория для сохранения результатов (создается автоматически)
```

## 🎯 Как это работает

1. **Инициализация**: Скрипт загружает YOLO модель (yolov8n по умолчанию)
2. **Конвертация**: Если модель OpenVINO не найдена, автоматически конвертируется из PyTorch
3. **Захват видео**: Открывается веб-камера и начинается захват кадров
4. **Детекция**: Каждый кадр обрабатывается моделью YOLO для обнаружения объектов
5. **Визуализация**: На кадр наносятся bounding boxes и метки классов
6. **Сохранение**: После достижения лимита кадров видео сохраняется в файл
7. **Копирование**: Если задан `HOST_OUTPUT_DIR`, файл копируется на хост
